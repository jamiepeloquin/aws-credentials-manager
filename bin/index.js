const appName = "AWS IAM Credentials Manager";
const requiredBinaries = ['aws'];
const debugAws = require('debug')('aws-sdk');

try {
    (async () => {

    })()
} catch (err) {
    display.error(err.message)
    process.exit(1)
}